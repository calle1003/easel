# ===============================
# Server Configuration
# ===============================
server.port=8080

# ===============================
# Application Custom Settings
# ===============================
# フロントエンドのURL (CORSやリダイレクト用)
app.frontend.url=http://localhost:5173

# ===============================
# Database Configuration (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/easel_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate Configuration
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Stripe Configuration
# ===============================
stripe.api.key=sk_test_51SbFZTHrC5XXQaL8rfJUhErHMdlWCIZk8HB3cbL704MSPviWIx2ISW3EBVrS05qtrqguOaHNrYUD30uHv6OvKoWa00aYNXFn1c
# Webhookシークレット（Stripeダッシュボードで取得）
# 開発環境では空のままでOK（署名検証がスキップされます）
stripe.webhook.secret=

# ===============================
# JWT Configuration
# ===============================
# シークレットキー（本番環境では必ず変更してください）
jwt.secret=easel-admin-secret-key-must-be-at-least-256-bits-long-for-security
# トークン有効期限（ミリ秒）: 24時間 = 86400000
jwt.expiration=86400000

# ===============================
# Mail Configuration
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Gmailを使用する場合はアプリパスワードを設定してください
# https://support.google.com/accounts/answer/185833
spring.mail.username=${MAIL_USERNAME:akimoto.takao9603@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# メール送信元の表示名
app.mail.from-name=easel